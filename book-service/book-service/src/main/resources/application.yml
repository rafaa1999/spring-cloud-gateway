server:
  port: 9001

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: book-service

logging:
  pattern:
    level: "%5p [${spring.application.name},%X{trace_id},%X{span_id}]"